<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.province.postdoctor.mapper.results_of_station.StatisticsNumberMapper">
    <select id="stationInfo" parameterType="com.province.postdoctor.entity.postdoctor_info.Postdoctorrinformation" resultType="com.province.postdoctor.entity.results_of_station.StatisticsNumber">
        SELECT
	d_name AS schoolname,
	/* 在站情况*/
	count(IF (STATUS = '在站',STATUS,NULL)) AS A,-- 在站
	count(IF (STATUS = '出站',STATUS,NULL)) AS B,-- 出站
	count(IF (STATUS = '退站',STATUS,NULL)) AS C-- 退站
FROM
	postdoctorrinformation
WHERE d_name = '太原科技大学'
GROUP BY
	d_name
    </select>

	<select id="list1"  resultType="java.util.Map">
 SELECT
	d_name AS schoolname,
        count(IF(`status`='在站',`status`,NULL)) as 在站,
        count(IF(`status`='出站',`status`,NULL)) as 出站,
        count(IF(`status`='退站',`status`,NULL)) as 退站,
        count(IF(`status`='注销',`status`,NULL)) as 注销
        FROM
        postdoctorrinformation
        where d_name='太原科技大学'
    </select>


</mapper>
